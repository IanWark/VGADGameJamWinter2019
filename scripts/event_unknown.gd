extends "res://scripts/sprite.gd"

func _ready():
	# Called when the node is added to the scene for the first time.
	# Initialization here
	scene = "res://scenes/event_unknown.tscn"
	locked_event = true

#func _process(delta):
#	# Called every frame. Delta is time since last frame.
#	# Update game logic here.
#	pass
